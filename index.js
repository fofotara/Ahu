/**
 * @format
 */

import {AppRegistry} from 'react-native';
import NavBar from './src/MainNavigation';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => NavBar);
